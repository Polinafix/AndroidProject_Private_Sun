<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.privatesun2">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Shopping_Cart" />
        <activity android:name=".Detailed_Menu" />
        <activity android:name=".Larger_Picture" />
        <activity android:name=".ShoppingCart" />
        <activity android:name=".ProceedOrder" />
        <activity android:name=".NavigationDrawerActivity" />
        <activity android:name=".FavouritesList" />
        <activity android:name=".ConfirmationActivity"></activity>
    </application>

</manifest>